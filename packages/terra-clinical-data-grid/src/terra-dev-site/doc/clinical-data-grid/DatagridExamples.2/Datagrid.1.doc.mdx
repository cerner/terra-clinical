import Datagrid from './Datagrid?dev-site-example';

<Datagrid description="This is a basic example featuring pinned columns and overflow in both directions. 
A default column width of 250px is used, but Column 0 is using an explicit 400px width. 
Every 5th row has an explicit 7rem height." />

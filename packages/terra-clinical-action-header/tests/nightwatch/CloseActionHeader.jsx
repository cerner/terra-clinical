import React from 'react';
import ActionHeader from '../../src/ActionHeader';

const CloseHeader = () => (
  <ActionHeader
    title="Action Header"
    onClose={() => 1}
  />
);

export default CloseHeader;

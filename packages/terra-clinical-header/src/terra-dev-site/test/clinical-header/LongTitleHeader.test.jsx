import React from 'react';

import Header from '../../../Header';

const LongTitleHeader = <Header id="Header" title="This is a header with long text .This is an element provides a customizable header layout with a prominent title options for the elements.This component saves time and effort in designing a header from scratch and ensures consistency across a website's pages." />;

export default () => LongTitleHeader;

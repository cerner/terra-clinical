language: generic
sudo: required
services:
  - docker
branches:
  only:
    - master
cache:
  directories:
    - $HOME/$TRAVIS_BUILD_ID
env:
  global:
    - DOCKER_IMAGE='cerner/terra-clinical'
    - TAG="${TRAVIS_PULL_REQUEST_SHA:=latest}"
jobs:
  include:
    - stage: build
      script:
        - travis_wait docker-compose build test-ci
        - travis_wait docker-compose --verbose pull standalone-chrome
        - travis_wait docker-compose run test-ci npm install
    - stage: print!
      script:
        - ls -al $HOME
        - ls -al $HOME/$TRAVIS_BUILD_ID
    - stage: clean up cache
      script:
        - sudo chown -R travis:travis $HOME/$TRAVIS_BUILD_ID
        - 'rm -rf $HOME/$TRAVIS_BUILD_ID
stages:
  - build
  - print!
  - clean up cache
